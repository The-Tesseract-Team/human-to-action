# Standalone skills

This directory is reserved for skills that install and operate independently of a plugin.

Skills bundled with a plugin belong under `plugins/<plugin-name>/skills/`. HumanToAction's `refine-agent-prompts` skill therefore lives inside `plugins/human-to-action/` and is not duplicated here.
